# Workflow Orchestration

Use subagents liberally to keep the main context clean. One task per agent.
After any correction, update `.claude/rules/lessons.md` (project-level only;
`~/.claude/rules/lessons.md` is user-curated).

> For plan mode etiquette, operating principles, session hygiene, and git commit
> conventions, invoke the `workflow-guidance` skill.
